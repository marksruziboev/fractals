
#include "fractol.h"

/*This function projects [-2, 2]^2 square to the window of pixels*/
t_vec  project(int q1, int q2)
{
    t_vec  s;
    
    s->x = ((double)q1)/200);
    s->y = ((double)q2)/200);
    return (s);
}